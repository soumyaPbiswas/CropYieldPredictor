# Crop Yield Prediction App

This Flask app predicts crop yield based on rainfall, temperature, and pesticide use using a trained Random Forest model to obtain low bias and low variance, thus resulting in increased accuracy.

## Setup

```bash
pip install -r requirements.txt
